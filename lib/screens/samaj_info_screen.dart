import 'package:flutter/material.dart';

class SamajInfoScreen extends StatelessWidget {
  const SamajInfoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Samaj Info"),
      ),
    );
  }
}
